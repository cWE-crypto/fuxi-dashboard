# MES 推广链接数据采集 - 探查阶段

## 当前进度
- [x] 已写 `mes_explore.py`：扫码登录 + Cookie 保存 + 全选下载 + 链接名称解析
- [x] 已写 `一键探查MES.bat`：双击启动
- [ ] **待你执行**：双击 `一键探查MES.bat`，扫码登录一次
- [ ] 探查产出后,我才能写正式的 `mes_collector.py`

## 你需要做的事（30秒搞定）
1. 双击 `C:\Users\liuxiaoxiao11\WorkBuddy\加好友看板\migrate\deploy\一键探查MES.bat`
2. 弹出 Chrome 窗口,自动打开 MES
3. 弹出二维码（首次）/ 已登录（cookie 还有效）
4. **首次**：打开微信扫一扫 → 扫页面上的二维码 → 确认登录
5. 脚本会自动：保存 cookie → 进入推广链接页面 → 全选 → 下载 → 解析文件结构
6. 弹出 "按 Enter 关闭" 时按一下回车
7. 把 logs/ 目录下的内容（特别是下载到的文件、截图）告诉我,或者直接告诉我文件路径

## 脚本产物（运行后会在 logs/ 出现）
| 文件 | 内容 |
|---|---|
| `01_mes_initial.png` | 打开 MES 时的截图 |
| `02_promotion_page.png` | 进入推广链接页面后的截图 |
| `mes_cookies.json` | 保存的登录 cookie（后续采集复用）|
| `sample_*.xlsx` / `sample_*.csv` | 下载到的推广链接明细文件 |
| `parsed_samples.json` | 链接名称解析样本（小组/类型/主播拆分） |

## 链接名称解析规则（已写在脚本里）
样本：`自孵化-郑州五组（锦安）-入团资料-屈海连`

| 段位 | 含义 | 样本值 |
|---|---|---|
| 第1段 | 大类 | 自孵化 |
| 第2段 | 小组 | 郑州五组（锦安） |
| 第3段 | 内容类型 | 入团资料 |
| 第4段 | 主播名称 | 屈海连 |

脚本会处理 1~4+ 段的命名,中间段会自动合并。

## 下一步（等你跑完探查后）
1. 拿到下载文件 → 看清楚 Excel/CSV 字段
2. 我写正式的 `mes_collector.py`：自动复用 cookie → 进入页面 → 全选下载 → 解析 → 产出 `data/mes_data.json`
3. 改造 `index.html` / `app.js` 适配新字段
4. 改造 GAS 定时调用脚本
5. 端到端验证后,删除旧 fuxi_collector.py