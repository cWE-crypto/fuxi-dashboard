# 加好友看板免费部署教程（GitHub Pages）

> 全程不用写代码，点点点 + 复制粘贴，20 分钟搞定。
> 迁移完成后：看板完全免费托管，每小时自动更新数据，**不再需要妙搭积分，也不再需要 Codex 定时发「更新」**。

---

## 迁移后的架构（知道在干嘛就行）

```
GitHub Actions（免费服务器，每小时自动跑一次）
    ↓
采集脚本登录伏羲、抓取加好友数据
    ↓
更新 data/fuxi_data.json 并自动提交
    ↓
GitHub Pages 自动更新（静态托管，免费）
    ↓
任何人打开链接就能看到最新数据（页面每 2 分钟自动刷新）
```

---

## 你需要准备

1. 一个 **GitHub 账号**（没有的话 1 分钟注册，免费）
2. 伏羲系统的**账号和密码**（采集数据要用）
3. 本教程对应的 **migrate/ 文件夹**（我已经帮你准备好了，里面有看板全部文件）

---

## 第一步：注册 GitHub 账号（2 分钟）

1. 浏览器打开：https://github.com
2. 点右上角 **Sign up**
3. 填邮箱 → 设密码 → 起用户名（比如 `yourname`）
4. 按提示完成验证（可能要去邮箱点确认链接）
5. 注册完自动登录

> 已有账号的话直接登录即可，跳过这步。

---

## 第二步：创建仓库（3 分钟）

1. 登录后，点右上角 **+** → **New repository**
2. 填写：
   - Repository name（仓库名）：**`koc-dashboard`**（可以自己起名，英文）
   - 选 **Public**（公开）或 **Private**（私有）都行，推荐 Public（免费托管）
   - 其他不用动
3. 点底部绿色按钮 **Create repository**

---

## 第三步：上传看板文件（5 分钟）

### 3.1 打包本地文件

`migrate/` 文件夹里这些文件都要传到仓库：

```
index.html            ← 看板页面
app.js                ← 前端逻辑
data.js               ← 示例数据
style.css             ← 样式
thumbnail.png         ← 图标
data/fuxi_data.json   ← 数据文件（文件夹 data 要一起）
deploy/               ← 采集脚本文件夹（整个文件夹）
.github/              ← 定时任务配置（整个文件夹，注意开头有个点）
README.md
```

### 3.2 上传

1. 在刚创建的仓库页面，点 **Add file** → **Upload files**
2. 把 `index.html`、`app.js`、`data.js`、`style.css`、`thumbnail.png`、`README.md` 拖进去
3. 再点 **Add file** → **Upload files**，把 `data` 文件夹里的 `fuxi_data.json` 传进去
   - 小技巧：直接把 `migrate/data/` 文件夹拖进上传区，GitHub 会自动建 `data/` 目录
4. 点 **Add file** → **Upload files**，把 `deploy` 文件夹整个拖进去（里面是 `fuxi_collector.py` 和 `requirements.txt`）
5. 最后传 `.github` 文件夹：把 `migrate/.github/` 整个文件夹拖进上传区
   - 注意：`.github` 是隐藏文件夹，在本地文件管理器里可能看不到。如果看不到：
     - 按 `Win+E` 打开文件管理器 → 顶部「查看」→ 勾选「隐藏的项目」
     - 或者用压缩包方式：把 `migrate` 文件夹压缩成 zip → 在 GitHub 网页「Upload files」拖 zip 时，GitHub 会自动解压展开（如果没展开就手动新建文件夹）
6. 每次上传完，点底部绿色按钮 **Commit changes**

> 💡 **最省事的方法**：把整个 `migrate` 文件夹压缩成 zip，然后在 GitHub 仓库页面直接拖 zip 上传，GitHub 会自动解压。如果没自动解压，就按上面 3.2 分步传。

---

## 第四步：配置伏羲账号密码（2 分钟）

1. 在仓库页面，点顶部的 **Settings**
2. 左边菜单往下拉，找到 **Secrets and variables** → **Actions**
3. 点 **New repository secret**，添加两个：
   - Name: `FUXI_USERNAME` → Secret: 你的伏羲账号
   - Name: `FUXI_PASSWORD` → Secret: 你的伏羲密码
4. 每个填完点 **Add secret**

---

## 第五步：允许定时任务自动提交（30 秒，很重要）

1. 还是在仓库 **Settings** 里
2. 左边菜单找 **Actions** → **General**（注意是 Actions 下面的 General，不是单独的 General）
3. 往下拉找到 **Workflow permissions**
4. 勾选 **Read and write permissions**
5. 点 **Save**

> ⚠️ 这步不做的话，定时任务采集完数据但提交不了，数据不会更新。

---

## 第六步：开启 GitHub Pages（2 分钟）

1. 仓库 **Settings** → 左边菜单找 **Pages**
2. **Build and deployment** → **Source** 选 **Deploy from a branch**
3. **Branch** 选 **main** → 文件夹选 **/ (root)**
4. 点 **Save**
5. 等 1 分钟，页面上方会出现你的看板地址，长这样：
   `https://你的用户名.github.io/koc-dashboard/`
6. 打开这个地址 → 看板应该正常显示了 ✅

---

## 第七步：手动跑一次采集，验证自动更新（3 分钟）

1. 仓库页面点顶部 **Actions**
2. 左边选 **伏羲数据定时采集**
3. 右边点 **Run workflow** → 绿色 **Run workflow** 按钮
4. 等 1-2 分钟，看列表里出现一个黄色的在跑
5. 跑完变绿色 ✓ 成功；变红色 ✗ 失败（点进去看日志，把红字错误发给我帮你排查）

**验证数据更新**：跑完后刷新看板页面 → 右上角「更新时间」应该变成刚才的时间。

---

## 搞定！以后完全自动

- 每小时整点，GitHub 自动跑一次采集 → 更新数据 → 看板自动刷新
- 关电脑、关飞书、关页面都在跑 ✅
- 打开看板链接，页面每 2 分钟自动拉最新数据，无需手动刷新 ✅
- 不再消耗任何妙搭积分 ✅
- 不再需要 Codex 定时发「更新」✅

---

## 常见问题

### Q: GitHub 免费吗？会不会突然收费？
A: GitHub Pages 和 Actions 都有免费额度。Actions 免费 2000 分钟/月，这个任务每小时跑 1 次约 10 分钟，一个月约 7200 分钟……哦等等，我重新算一下：每小时跑 1 次，1 次约 1-2 分钟，一天 24-48 分钟，一个月约 720-1440 分钟，**在免费额度内**。如果担心超了，可以把采集频率改成每 2 小时一次（教程第八步）。

### Q: 伏羲账号密码放 GitHub 安全吗？
A: 安全。GitHub Secrets 加密存储，谁也看不到（包括你自己），只在定时任务运行时临时注入。

### Q: 想改成每 2 小时更新一次？
A: 打开仓库里的 `.github/workflows/fuxi-collect.yml`，把 `cron: '0 * * * *'` 改成 `cron: '0 */2 * * *'`，保存后自动生效。

### Q: 原来的妙搭看板怎么办？
A: 确认 GitHub 版正常后，妙搭那份可以停用，不再消耗积分。

### Q: 部署的链接想分享给同事？
A: 直接把 GitHub Pages 地址发给同事即可，打开就能看，不需要登录。

---

## 卡住了怎么办？

哪一步不会，直接截图发给我，我告诉你下一步点哪里。
