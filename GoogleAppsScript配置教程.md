# Google Apps Script 定时触发器配置教程（约 10 分钟）

> 解决什么问题：GitHub 免费计划的定时任务（cron）会被平台**随机静默丢弃**（官方不保证执行）。
> 本方案用 **Google 免费云端定时服务** 每小时自动调用 GitHub 接口触发采集，**不依赖你的电脑，云端跑，可靠**。

---

## 整体原理

```
Google Apps Script（云端定时，每小时）
        │  调 GitHub API（workflow dispatch）
        ▼
GitHub Actions（采集伏羲数据 → 更新数据 → 部署 Pages）
        ▼
你的看板（https://cwe-crypto.github.io/fuxi-dashboard/）每小时自动更新
```

---

## 准备工作

- 一个 **Google/Gmail 账号**（没有的话去 https://accounts.google.com/signup 免费注册，2 分钟）

---

## 第一步：打开 Google Apps Script 新建项目

1. 浏览器打开 **https://script.google.com**
2. 点左上角 **「+ 新建项目」**（或「新项目」）
3. 把默认代码全部**删掉**

## 第二步：粘贴代码并填 Token

1. 打开仓库里的 **`deploy/google-apps-script.js`** 文件，全选复制全部内容
2. 粘贴到 Google 的编辑框里
3. 找到第 20 行左右这一行：
   ```javascript
   var GITHUB_TOKEN = '在此粘贴你的TOKEN';
   ```
4. 把 `'在此粘贴你的TOKEN'` 替换成 **AI 助手给你的那个 Token**（`gho_` 开头的那串，直接粘到引号里）
5. 点工具栏 **💾 保存**，项目名改成：`加好友看板定时触发器`

## 第三步：测试 Token 是否有效

1. 编辑器顶部函数下拉框里选择 **`testConnection`**
2. 点 **▶ 运行**
3. 第一次运行会弹授权窗口：
   - 选你的 Google 账号 → **「高级」→「前往 加好友看板定时触发器（不安全）」** → **「允许」**
   - （提示"不安全"是因为脚本是你自己写的、非 Google 审核应用，正常现象）
4. 点左侧 **「执行记录」**（时钟图标）查看日志：
   - 看到 `✅ Token 有效！仓库内定时任务：伏羲数据定时采集(active)` → 成功，继续下一步
   - 看到 `❌ HTTP 401/403` → 把错误信息发给 AI 助手

## 第四步：设置每小时定时触发器

1. 点左侧 **「触发器」**（闹钟图标）→ 右下角 **「+ 添加触发器」**
2. 按下图配置：

   | 项目 | 选择 |
   |---|---|
   | 选择要运行的函数 | **`triggerCollect`** |
   | 选择运行部署 | **Head** |
   | 选择事件源 | **按时间** |
   | 选择时间类型 | **小时计时器** |
   | 选择小时范围 | **每小时**（或每 30 分钟，见下方说明） |
   | 失败通知设置 | **立即通知**（推荐，出问题会邮件提醒你） |

3. 点 **保存**

> 💡 **频率建议**：默认「每小时」即可（数据最多滞后 1 小时，免费配额足够）。
> 想要更频繁，选「分钟计时器 → 每 30 分钟」，配额也够用（每月约 1440 次调用，远低于免费上限）。

## 第五步：验证整条链路

1. 回到编辑器，函数下拉框选 **`triggerCollect`**，点 **▶ 运行**（手动跑一次）
2. 去 **执行记录** 看日志：应看到 `✅ 触发成功（HTTP 204）`
3. 打开 GitHub 仓库页面 → **Actions** 标签 → 应看到一条新的运行记录在跑
4. 约 2 分钟后刷新看板 → 更新时间变成刚才的时间

---

## 常见问题

### Q1：Token 过期了怎么办？
GitHub 的登录会话 Token 有时效（gho_ 开头的一般 8 小时~1 天有效）。
**重要：这个方案长期运行需要稳定的 Token。** 推荐之后（今天跑通后）在 GitHub 网页生成一个**长期专用 Token**（见下方"进阶：换成专用 Token"），30~90 天内有效，到期换一次即可。

### Q2：触发失败 HTTP 401/403？
- 401 = Token 无效或已过期 → 换新 Token
- 403 = Token 权限不足 → 需要 `repo` 权限（生成时勾选）

### Q3：会重复触发浪费配额吗？
不会。脚本内置**防重复逻辑**：30 分钟内已触发过就跳过。手动点运行 + 定时触发不会叠加。

### Q4：Google 免费额度够吗？
够。Apps Script 免费版每天可执行约 2 万次定时触发器，我们每小时 1 次（每月约 720 次），不到 4%。

### Q5：电脑关机还能跑吗？
**能。** 整个链路跑在 Google + GitHub 的云上，跟你电脑完全无关。

---

## 进阶：换成专用 Token（长期稳定，推荐跑通后做一次）

当前用的 Token 是 AI 助手登录会话的临时凭证，会过期。为了长期稳定，建议生成专用 Token：

1. 打开 **https://github.com/settings/tokens** → **Generate new token (classic)**
2. Note 填：`fuxi-dashboard-scheduler`
3. Expiration 选 **90 days**（到期前记得重新生成替换）
4. **勾选 `repo`**（整个框）——这是触发 workflow 的唯一必需权限
5. 点 **Generate token** → **立刻复制**（只显示一次！）
6. 回到 Google Apps Script，把第 20 行的 Token 换成新的，保存即可
7. 旧 Token 可以在 GitHub 设置里 **Revoke**（吊销），双保险

> ⚠️ 安全提醒：Token 相当于你 GitHub 账号的一把钥匙。只粘贴到**你自己的** Google 脚本里，
> 不要发到任何聊天群、不要在公开网页粘贴。AI 助手会直接帮你生成，不需要你再操作。
